# run-sql + get-sql-instance 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 dw-cli 中新增 `run-sql`（直连 MaxCompute 执行 SQL）与 `get-sql-instance`（跟进超时降级的 instance）两个命令。

**Architecture:** 复用现有 `core/odps_client.py` 的 `build_odps(project)` 连接层（与 list-tables 共用）。新建 `dw_cli/commands/sql.py` 模块承载两个命令。run-sql 内部：`o.execute_sql(sql)` 同步执行 + 15s 心跳 + 软超时降级；按 SQL 关键字前缀判写操作需 `--confirm`；按 reader 实际结构输出结果；logview 输出前做私有云地址替换（`cloud:80`→`cloud-inner`）。get-sql-instance：`o.get_instance(id)` 查状态，已完成则输出结果。

**Tech Stack:** Python + Typer + PyODPS（惰性 import，复用 odps_client）+ 现有 core（output/errors/load_arg/auth_params）。

**设计依据:** `docs/superpowers/specs/2026-07-09-run-sql-design.md`

---

## 文件结构

- **Create:** `dw-cli/dw_cli/commands/sql.py` — 新模块，承载 `run-sql` + `get-sql-instance`，含 logview 替换工具函数。
- **Modify:** `dw-cli/dw_cli/main.py` — 注册 sql_cmds 模块，加面板 + _CMD_PANELS 映射。
- **Modify:** `docs/dw-cli-封装注意事项.md` — 补 run-sql/logview 替换的私有云特性记录。

> 注：dw-cli 现无测试框架（无 tests/ 目录），本计划采用真调验证（用户已授权在 32890 空间测试），不新增单测。

---

### Task 1: 创建 sql.py 模块骨架 + logview 替换工具

**Files:**
- Create: `dw-cli/dw_cli/commands/sql.py`

- [ ] **Step 1: 创建 sql.py，写 logview 替换函数 + 模块头**

创建 `dw-cli/dw_cli/commands/sql.py`：

```python
# -*- coding: utf-8 -*-
"""sql 类命令（spec §9 按资源分文件，对外平铺）。

run-sql：直连 MaxCompute 执行 SQL（PyODPS），私有云可用。
get-sql-instance：跟进超时降级的 instance 状态 + 取结果。

⚠️ run-sql 走 PyODPS 直连（与 list-tables 共用 core/odps_client.build_odps），
   不是 DataWorks OpenAPI（OpenAPI 无"执行任意 SQL"方法）。

⚠️ 安全：按 SQL 关键字前缀判写操作（DROP/INSERT/CREATE/...），写需 --confirm。
   不共用 confirm.py 的 delete_ 前缀机制（那是 SDK 方法名，套不上 SQL）。

⚠️ logview 地址替换（私有云特性，2026-07-09 真调验证）：
   PyODPS 生成的 logview h= 带 odps.cloud.zj.gov.cn:80，
   token 是 cloud-inner 签发，直接打开报 token malformed。
   需替换为 odps.cloud-inner.zj.gov.cn/api。
"""
from __future__ import annotations

import re
import sys
import threading
import time
from typing import Optional

import typer

from dw_cli.core import errors, odps_client, output
from dw_cli.core.load_arg import load_arg
from dw_cli.commands import auth_params, output_option, query_option

app = typer.Typer(help="sql 类命令（MaxCompute SQL 直连执行）")

# ── logview 地址替换（私有云特性） ───────────────────────────────────────────
_LOGVIEW_FROM = "odps.cloud.zj.gov.cn:80/api"
_LOGVIEW_TO = "odps.cloud-inner.zj.gov.cn/api"


def fix_logview(url: str) -> str:
    """替换 logview 中 h= 参数的 host（cloud:80 → cloud-inner）。

    PyODPS 生成的 logview，h= 带 ODPS_ENDPOINT 的 cloud:80，
    但 token 用 cloud-inner 签发，直接打开报 token malformed。
    替换后与 DataWorks 页面手动执行的 logview 一致（已真调验证）。
    """
    if not url:
        return url
    return url.replace(_LOGVIEW_FROM, _LOGVIEW_TO)


# ── SQL 写操作关键字判定（B2 细粒度） ────────────────────────────────────────
_WRITE_KEYWORDS = (
    "DROP", "TRUNCATE", "DELETE", "INSERT", "UPDATE",
    "CREATE", "ALTER", "MERGE", "RENAME",
)
_WRITE_RE = re.compile(
    r"^\s*(" + "|".join(_WRITE_KEYWORDS) + r")\b",
    re.IGNORECASE,
)


def is_write_sql(sql: str) -> bool:
    """按 SQL 前缀关键字判定是否写操作（B2）。"""
    return bool(_WRITE_RE.match(sql or ""))
```

- [ ] **Step 2: 真调验证 logview 替换 + 写判定**

运行：

```powershell
cd D:\work\10openapi\dw-cli
python -c "import io,sys; sys.stdout=io.TextIOWrapper(sys.stdout.buffer,encoding='utf-8'); from dw_cli.commands.sql import fix_logview, is_write_sql; orig='http://logview.../?h=http://service.cn-hangzhou-zjzwy01-d01.odps.cloud.zj.gov.cn:80/api&p=dqsc_prod&i=xxx&token=yyy'; print('FIXED h:', fix_logview(orig).split('h=')[1].split('&')[0]); print('select:', is_write_sql('select * from t')); print('drop:', is_write_sql('drop table t')); print('insert:', is_write_sql('  INSERT into t'))"
```

Expected:
```
FIXED h: http://service.cn-hangzhou-zjzwy01-d01.odps.cloud-inner.zj.gov.cn/api
select: False
drop: True
insert: True
```

- [ ] **Step 3: Commit**

```bash
git add dw-cli/dw_cli/commands/sql.py
git commit -m "feat(run-sql): sql.py 骨架 + logview替换 + SQL写判定工具函数"
```

---

### Task 2: run-sql 核心执行 + 心跳 + 超时降级

**Files:**
- Modify: `dw-cli/dw_cli/commands/sql.py`

- [ ] **Step 1: 追加 run-sql 命令实现**

在 `dw-cli/dw_cli/commands/sql.py` 末尾追加：

```python
def _read_results(instance, limit: int) -> dict:
    """从 instance 的 reader 读结果，按 reader 实际结构返回。

    - SELECT：有 schema → {columns, rows, truncated, total}
    - DESC/SHOW 等元信息：reader 有行但结构不同 → 原样按行输出
    - 无结果集（INSERT/DDL/DML）：reader 无 schema 无行 → 返回 None（调用方报状态）

    具体列结构/截断层留实现时真调确认，此处先按 open_reader 通用形态写。
    """
    try:
        reader = instance.open_reader()
    except Exception:
        # 无结果集（INSERT/DDL）open_reader 可能抛异常或无 schema
        return None

    columns = []
    if hasattr(reader, "_schema") and reader._schema is not None:
        columns = [c.name for c in reader._schema.columns]

    rows = []
    truncated = False
    total = 0
    for row in reader:
        total += 1
        if len(rows) >= limit:
            truncated = True
            continue
        vals = list(row.values) if hasattr(row, "values") else list(row)
        rows.append(vals)

    if not columns and not rows:
        return None

    return {
        "columns": columns,
        "rows": rows,
        "truncated": truncated,
        "total": total,
    }


def _heartbeat(instance, stop_event: threading.Event, project: str) -> None:
    """每 15s 往 stderr 输出心跳（不污染 stdout 结果）。"""
    elapsed = 0
    logview = ""
    try:
        logview = fix_logview(instance.get_logview_address())
    except Exception:
        pass
    while not stop_event.wait(15):
        elapsed += 15
        sys.stderr.write(
            f"[run-sql] 运行中，已 {elapsed}s，instance_id={instance.id}，"
            f"logview={logview}\n"
        )
        sys.stderr.flush()


@app.command("run-sql")
def run_sql(
    ctx: typer.Context,
    project: str = typer.Option(..., "--project",
        help="MaxCompute 项目名（如 dqsc_prod），与 list-tables --odps-project 同构"),
    sql: str = typer.Option(..., "--sql",
        help="SQL 语句；或 file://query.sql 读文件（复用 load_arg）"),
    limit: int = typer.Option(100, "--limit",
        help="结果行上限，默认 100 防爆上下文；建议 ≤1000"),
    timeout: int = typer.Option(180, "--timeout",
        help="软超时秒数，默认 180；0 表示不限（纯同步等到底）"),
    no_wait: bool = typer.Option(False, "--no-wait",
        help="提交后立即返回 instance_id + logview（强制异步）"),
    confirm_flag: bool = typer.Option(False, "--confirm",
        help="写操作（DROP/INSERT/CREATE/...）必须显式确认"),
    query: Optional[str] = query_option(),
    output_fmt: str = output_option(),
):
    """直连 MaxCompute 执行 SQL（PyODPS，私有云可用）。

    \b
    🚀 Examples:
      # 读操作（SELECT）默认放行
      dw-cli run-sql --project dqsc_prod \\
        --sql "select * from t00_ods_table_cols_list limit 3"

      # 长脚本走 file://
      dw-cli run-sql --project dqsc_prod --sql file://query.sql

      # 写操作需 --confirm
      dw-cli run-sql --project dqsc_prod \\
        --sql "insert into t select * from s" --confirm

    \b
    📦 Output JSON Structure:
      - SELECT:    {columns, rows, truncated, total, instance_id, logview}
      - 写/DDL:    {success, instance_id, logview}
      - 超时降级:  {status:"timeout", instance_id, logview, message}
      - --no-wait: {status:"submitted", instance_id, logview}

    \b
    ⚠️ 私有云特性：
      - logview 自动替换 h= 的 cloud:80 → cloud-inner（token 才能校验通过）。
      - pyodps 缺失报 MissingDependency/exit 2，不影响其它命令。
    """
    sql_text = load_arg(sql)

    # B2 写判定
    if is_write_sql(sql_text) and not confirm_flag:
        raise errors.DwCliError(
            "写操作 SQL 需要 --confirm：加 --confirm 执行。",
            code="NeedsConfirm",
            category=errors.CATEGORY_USAGE,
            recommend="run-sql 写操作（DROP/INSERT/CREATE/ALTER/...）默认拒绝，加 --confirm 执行。",
        )

    auth = auth_params(ctx)
    try:
        o = odps_client.build_odps(project, **auth)
    except Exception as error:
        errors.fail(error)
        return

    # 提交 SQL（execute_sql 本身同步阻塞，但先拿 instance 再说）
    try:
        instance = o.execute_sql(sql_text)
    except Exception as error:
        errors.fail(error)
        return

    logview = fix_logview(_safe_logview(instance))

    # --no-wait：提交即返回
    if no_wait:
        result = {
            "status": "submitted",
            "instance_id": instance.id,
            "logview": logview,
        }
        output.emit(result, query=query, output=output_fmt)
        return

    # --timeout 0：纯同步，不心跳不降级
    if timeout == 0:
        _finish_and_emit(instance, limit, query, output_fmt, logview)
        return

    # 默认：心跳 + 软超时降级
    stop_event = threading.Event()
    hb = threading.Thread(
        target=_heartbeat, args=(instance, stop_event, project), daemon=True
    )
    hb.start()
    try:
        done = _wait_instance(instance, timeout)
    finally:
        stop_event.set()
        hb.join(timeout=1)

    if not done:
        result = {
            "status": "timeout",
            "instance_id": instance.id,
            "logview": logview,
            "message": "SQL 仍在运行，可用 get-sql-instance --instance-id 跟进",
        }
        output.emit(result, query=query, output=output_fmt)
        return

    _finish_and_emit(instance, limit, query, output_fmt, logview)


def _safe_logview(instance) -> str:
    """安全取 logview（失败返回空串，不阻断执行）。"""
    try:
        return instance.get_logview_address() or ""
    except Exception:
        return ""


def _wait_instance(instance, timeout: int) -> bool:
    """等待 instance 完成，超时返回 False。"""
    deadline = time.time() + timeout
    while time.time() < deadline:
        try:
            if instance.is_successful() or instance.status in (
                "Terminated", "STATUS_TERMINATED",
            ):
                return True
            # 失败也算"完成"（交由 _finish_and_emit 报状态）
            if str(instance.status).upper().startswith("FAIL"):
                return True
        except Exception:
            pass
        time.sleep(2)
    return False


def _finish_and_emit(instance, limit, query, output_fmt, logview) -> None:
    """instance 完成后：读结果或报状态，统一输出。"""
    results = _read_results(instance, limit)
    if results is None:
        # 无结果集（INSERT/DDL/DML）
        success = False
        try:
            success = bool(instance.is_successful())
        except Exception:
            success = str(instance.status).upper() not in (
                "FAILED", "STATUS_FAILED",
            )
        result = {
            "success": success,
            "instance_id": instance.id,
            "logview": logview,
            "status": str(instance.status),
        }
    else:
        result = {
            "columns": results["columns"],
            "rows": results["rows"],
            "truncated": results["truncated"],
            "total": results["total"],
            "instance_id": instance.id,
            "logview": logview,
        }
    output.emit(result, query=query, output=output_fmt)
```

- [ ] **Step 2: 真调验证 run-sql SELECT**

运行：

```powershell
cd D:\work\10openapi\dw-cli
dw-cli run-sql --project dqsc_prod --sql "select * from t00_ods_table_cols_list limit 3"
```

Expected: JSON 输出含 `columns`、`rows`（3 行）、`instance_id`、`logview`（h= 已是 cloud-inner）。退出码 0。

- [ ] **Step 3: 真调验证 logview 可在浏览器打开**

把输出的 logview 复制到浏览器，确认能正常打开执行详情（与之前真调一致）。

- [ ] **Step 4: 真调验证写操作拦截**

运行：

```powershell
dw-cli run-sql --project dqsc_prod --sql "drop table t00_ods_table_cols_list"
```

Expected: 退出码 2，报 `NeedsConfirm`。加 `--confirm` 后真执行（⚠️ 这会真删表，**跳过此步**，只验证拦截生效）。

- [ ] **Step 5: Commit**

```bash
git add dw-cli/dw_cli/commands/sql.py
git commit -m "feat(run-sql): 核心执行 + 15s心跳 + 软超时降级 + B2写拦截 + logview替换"
```

---

### Task 3: get-sql-instance 命令

**Files:**
- Modify: `dw-cli/dw_cli/commands/sql.py`

- [ ] **Step 1: 追加 get-sql-instance 实现**

在 `dw-cli/dw_cli/commands/sql.py` 末尾追加：

```python
@app.command("get-sql-instance")
def get_sql_instance(
    ctx: typer.Context,
    instance_id: str = typer.Option(..., "--instance-id",
        help="MaxCompute instance ID（run-sql 超时降级返回的 instance_id）"),
    project: str = typer.Option(..., "--project",
        help="MaxCompute 项目名（与 run-sql --project 一致）"),
    limit: int = typer.Option(100, "--limit",
        help="结果行上限（仅 instance 完成且有结果集时生效）"),
    query: Optional[str] = query_option(),
    output_fmt: str = output_option(),
):
    """跟进 MaxCompute instance 状态 + 取结果（run-sql 超时降级闭环）。

    \b
    🚀 Examples:
      # run-sql 超时降级后跟进
      dw-cli get-sql-instance --instance-id 20260709092621464gt655m43 \\
        --project dqsc_prod

    \b
    📦 Output JSON Structure:
      - 运行中:  {status:"running", instance_id, logview}
      - 完成+结果: {columns, rows, truncated, total, instance_id, logview}
      - 完成+无结果: {success, status, instance_id, logview}

    \b
    ⚠️ 复用 run-sql 的结果读取逻辑（_read_results）与 logview 替换（fix_logview）。
    """
    auth = auth_params(ctx)
    try:
        o = odps_client.build_odps(project, **auth)
        instance = o.get_instance(instance_id)
    except Exception as error:
        errors.fail(error)
        return

    logview = fix_logview(_safe_logview(instance))
    status_str = str(instance.status)
    status_upper = status_str.upper()

    # 判断是否完成
    is_done = (
        "TERMINATED" in status_upper
        or status_upper.startswith("FAIL")
    )
    if not is_done:
        result = {
            "status": "running",
            "instance_id": instance_id,
            "logview": logview,
            "raw_status": status_str,
        }
        output.emit(result, query=query, output=output_fmt)
        return

    # 已完成：读结果或报状态
    results = _read_results(instance, limit)
    if results is None:
        success = False
        try:
            success = bool(instance.is_successful())
        except Exception:
            success = not status_upper.startswith("FAIL")
        result = {
            "success": success,
            "status": status_str,
            "instance_id": instance_id,
            "logview": logview,
        }
    else:
        result = {
            "columns": results["columns"],
            "rows": results["rows"],
            "truncated": results["truncated"],
            "total": results["total"],
            "instance_id": instance_id,
            "logview": logview,
        }
    output.emit(result, query=query, output=output_fmt)
```

- [ ] **Step 2: 真调验证 get-sql-instance（已完成 instance）**

运行（用 Task 2 run-sql 产生的 instance_id）：

```powershell
dw-cli get-sql-instance --instance-id <上面run-sql返回的instance_id> --project dqsc_prod
```

Expected: JSON 输出含 `columns`、`rows`（3 行）、`instance_id`、`logview`，退出码 0。

- [ ] **Step 3: Commit**

```bash
git add dw-cli/dw_cli/commands/sql.py
git commit -m "feat(get-sql-instance): 跟进instance状态+取结果(超时降级闭环)"
```

---

### Task 4: 注册到 main.py + 面板

**Files:**
- Modify: `dw-cli/dw_cli/main.py`

- [ ] **Step 1: 加 import**

在 `main.py` 的 import 区（其它 `from dw_cli.commands import ... as ..._cmds` 附近）加一行：

```python
from dw_cli.commands import sql as sql_cmds
```

- [ ] **Step 2: 加面板常量**

在 `_PANEL_RAW = ...` 附近加：

```python
_PANEL_SQL = "🔧 SQL 直连执行"
```

- [ ] **Step 3: 加 _CMD_PANELS 映射**

在 `_CMD_PANELS` dict 里（`"raw": _PANEL_RAW,` 附近）加：

```python
    "run-sql": _PANEL_SQL,
    "get-sql-instance": _PANEL_SQL,
```

- [ ] **Step 4: 注册 app**

在 `app.add_typer(...)` 区（其它 `app.add_typer(xxx_cmds.app, name="")` 附近）加：

```python
app.add_typer(sql_cmds.app, name="")
```

- [ ] **Step 5: 真调验证 --help 分组**

运行：

```powershell
dw-cli --help
```

Expected: 顶层 help 出现 `🔧 SQL 直连执行` 面板，含 `run-sql` 和 `get-sql-instance`。

- [ ] **Step 6: 真调验证子命令 --help**

运行：

```powershell
dw-cli run-sql --help
dw-cli get-sql-instance --help
```

Expected: 两个命令的 help 正常显示（含 Examples / Output JSON Structure / 私有云特性）。

- [ ] **Step 7: Commit**

```bash
git add dw-cli/dw_cli/main.py
git commit -m "feat: 注册 run-sql/get-sql-instance 到 main.py + SQL直连面板"
```

---

### Task 5: 更新封装注意事项文档 + SDK 参数校验

**Files:**
- Modify: `docs/dw-cli-封装注意事项.md`

- [ ] **Step 1: 补 run-sql/logview 私有云特性**

在 `docs/dw-cli-封装注意事项.md` 合适位置（私有云特性区或 3d/3c 封装注意附近）追加：

```markdown
## run-sql / get-sql-instance（2026-07-09）

### logview 地址替换（私有云特性，已真调验证）
PyODPS 生成的 logview，h= 参数带 ODPS_ENDPOINT 的 `odps.cloud.zj.gov.cn:80/api`，
但 token 用 `cloud-inner` host 签发，直接打开报 `authentication failed: the bearer-token is malformed`。
需替换 h= 为 `odps.cloud-inner.zj.gov.cn/api`（其余参数原样）。
替换规则固化在 `commands/sql.py` 的 `fix_logview()`，run-sql / get-sql-instance 输出 logview 时统一调用。
原始地址 vs DataWorks 页面手动执行地址的区别就是这一个词（cloud → cloud-inner）。

### run-sql 安全边界（B2）
按 SQL 前缀关键字判写操作（DROP/TRUNCATE/DELETE/INSERT/UPDATE/CREATE/ALTER/MERGE/RENAME），
写需 --confirm。不共用 confirm.py 的 delete_ 前缀机制（那是 SDK 方法名，套不上 SQL）。

### run-sql 结果输出
按 reader 实际结构输出：SELECT → {columns,rows,truncated,total}；
DESC/SHOW 等元信息 → reader 原样按行（不强装 columns）；无结果集 → 只报 {success,status}。
默认 100 行 + --limit（建议≤1000），无翻页。

### run-sql 执行模式
同步 + 15s 心跳（stderr）+ 软超时 180s 降级（输出 instance_id+logview，exit 0）。
--timeout 0 不限；--no-wait 强制异步。超时降级后用 get-sql-instance 跟进。

### run-pyodps 已砍掉
改用 DataWorks pyodps 节点 + run-manual-dag-nodes + get-dag 工作流替代。
理由：代码作节点文件不进 CLI，安全；复用已封装 dag 命令；agent 可编排。
```

- [ ] **Step 2: 更新"待办：run-sql / run-pyodps"段落状态**

找到文档里之前的 `## 待办：run-sql / run-pyodps 命令` 段落，改为标注"run-sql 已实现，run-pyodps 已砍掉（见上）"。logview 延后项标记"已落地（fix_logview）"。

- [ ] **Step 3: Commit**

```bash
git add docs/dw-cli-封装注意事项.md
git commit -m "docs: 补 run-sql/logview替换私有云特性 + run-pyodps砍掉说明"
```

- [ ] **Step 4: 跑 SDK 参数校验脚本（确认无回归）**

运行：

```powershell
cd D:\work\10openapi
python scripts/check_sdk_params.py
```

Expected: 仍 75/75 aligned（run-sql/get-sql-instance 不走 SDK Request，不影响计数，但确认无回归）。

- [ ] **Step 5: 最终全链路真调**

运行（端到端）：

```powershell
cd D:\work\10openapi\dw-cli
# 1. SELECT
dw-cli run-sql --project dqsc_prod --sql "select * from t00_ods_table_cols_list limit 3"
# 2. 拿 instance_id 跟进
dw-cli get-sql-instance --instance-id <上一步的instance_id> --project dqsc_prod
# 3. 顶层 help 确认面板
dw-cli --help
```

Expected: 三步全通，SELECT 结果一致，面板显示正常。

---

## Self-Review

**Spec 覆盖：**
- 2.1 B2 安全 → Task 1 `is_write_sql` + Task 2 写判定 ✅
- 2.2 C 结果集（100+--limit，无翻页）→ Task 2 `--limit` ✅
- 2.3 reader 实际结构输出 → Task 2 `_read_results` ✅
- 2.4 内联+file://、选项参数 → Task 2 `--sql` + `load_arg` ✅
- 2.5 同步+心跳+超时降级 → Task 2 `_heartbeat`/`_wait_instance`/`--timeout`/`--no-wait` ✅
- 2.6 logview 替换 → Task 1 `fix_logview` ✅
- 2.7 get-sql-instance → Task 3 ✅
- 砍掉 run-pyodps → 文档说明（Task 5）✅

**Placeholder 扫描：** 无 TBD/TODO。`_read_results` 的列结构留实现真调，已在代码注释标注（非占位，是有意的延后）。

**类型一致性：** `fix_logview`/`is_write_sql`/`_read_results`/`_safe_logview`/`_wait_instance`/`_finish_and_emit` 在 Task 1-3 定义后一致引用。`_heartbeat` 在 Task 2 定义并使用。